{
  "name": "backend",
  "version": "0.0.1",
  "description": "\"\"",
  "main": "node index.js",
  "scripts": {
    "build:ui": "(if exist build rmdir /s /q build )",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build && git add . && git commit -m uibuild && npm run deploy",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "Part3"
  },
  "author": "Hakizu",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.16"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
